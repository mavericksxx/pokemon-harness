# backlog

Working list of known issues and planned work — open items only; completed work moves to [CHANGELOG.md](CHANGELOG.md). Rule of engagement: ship the release in flight first, then resolve items **one at a time** (no parallel fan-outs unless explicitly requested).

## in flight (v1.1.0 release batch)

- [x] long-text overflow (dictation/paths escaping roster cards, garden bubbles, topbar; brand under traffic lights — defensive fix, needs a visual once-over)
- [x] duplicate topbar view-mode buttons (terminal vs terminal-full merged)
- [x] settings panel redesign (pixel checkboxes/slider, segmented lowercase theme picker, terse copy)
- [x] light mode themes the terminals too (live, incl. system-mode OS flips); theme toggle in topbar
- [x] default zoom one notch out (−0.5); Cmd+0 resets to it too (custom app menu)
- [x] sound icon opens mini-player popover (mute, volume, transport, search, gen filter)
- [x] peaceful-only ambient music; battle tracks (379/1376 classified) reserved for battles; manual picks always win
- [x] app icon white ring in dock (full-bleed icns; pre-Tahoe macOS now shows square corners — accepted)
- [x] background subagents not appearing in the garden (root cause: the CLI reports every subagent dispatch as an instant async launch and emits no reliable completion hook; lifecycle no longer depends on those signals)
- [x] new subagent battle lifecycle: intro fight → loser wanders its own far corner while working → final fight on completion → faints (8-min safety fallback since the CLI emits no trustworthy completion event)
- [x] garden black-screen hardening: battle update loop can no longer kill the render frame; crash vector under concurrent battles removed (not a confirmed repro of the exact report — watch for recurrence)
- [x] verified: updates never lose sessions (atomic persistence flushed before quit, `--resume` respawn, userData path pinned by app name independent of bundle churn; update check never touches state)

Then: user QA pass → `node tools/release.cjs minor` → v1.1.0.

## next up (one at a time, in rough order)

1. **"tell chikorita to do X" routing** — SHIPPED (2026-08-29, see CHANGELOG unreleased). Follow-ups worth watching: bracketed-paste first-prompt delivery is unverified against a live CLI (flagged in code — first place to look if the persona lands fragmented); a relay landing in the narrow just-spawned window before a target CLI accepts input could be lost; arceus quoting the grammar verbatim would cosmetically toast an unresolved name.

## queued phases (logged 2026-08-29 — batch per phase, dispatch after usage limits ships)

### phase A — subagent lifecycle redesign — SHIPPED (2026-08-29, see CHANGELOG unreleased). Root cause of invisible/premature-death subagents found and fixed (Stop-never-signaled + shared-loop abort). Watch next real fan-out to confirm in the wild.

### phase B — layout/visual bugs — SHIPPED (2026-08-29, see CHANGELOG unreleased): icon normalization, taller topbar, quick-settings/gear merge, chip actions folded in, ghost strip, tool-bubble overlap, zoom/pixel-font root-cause fix. Needs user visual QA on the new chrome.

### phase C — ux polish — SHIPPED (2026-08-29, see CHANGELOG unreleased): species name on roster card, visible change-pokemon action, bigger picker, exact-species swap + keep-at-stage checkbox.

### phase D — feature: mega evolution

- megas are absent from the picker (they're battle forms, not dex entries — sprite sets do include mega forms). DECIDED (2026-08-29): **trigger = during battles only** — when a completion battle starts, the MAIN agent's pokemon mega evolves for the duration of the fight if its species has a mega form (sprite availability verified against the real sprite source; species without a mega just battle normally), then reverts after the battle/victory celebration. No picker entries, no manual button, no work-based trigger. Build AFTER the phase A lifecycle redesign lands (it owns the battle flow this hooks into).

### phase F — performance & battery optimization (requested 2026-08-29; runs BEFORE the demo/showreel phase)

- macOS battery menu lists Pokeharness.app under "Using Significant Energy" (screenshot-confirmed, even in Low Power mode on AC). Dedicated optimization pass so the app runs smooth AND cheap:
  - profile first (Instruments/Activity Monitor energy impact + chrome://tracing), then fix by evidence — likely suspects: the Pixi ticker rendering the garden at full fps continuously even when nothing moves or the window is hidden; render both when occluded/minimized (Electron `browserWindow.on('hide'/'show')`, `powerMonitor`, page visibility) — pause or drop to a low tick rate when not visible and when the scene is fully idle
  - cap the garden to a sane fps (pixel art doesn't need 120hz on ProMotion displays); consider dirty-flag rendering (only render frames where something animated)
  - audit timers/polls across main+renderer (update check, counters snapshot, cost watcher, usage poller, prefetchers) for coalescing; audit xterm.js renderer choice and output flow for busy sessions
  - the splitter ResizeObserver fix (phase B/wave 1) already removes one thrash source; verify with the log
  - success criteria: app leaves the "Using Significant Energy" list during normal idle use; garden CPU near-zero when window hidden
- ties into the existing "load-test 15+ concurrent chatty sessions" item in bigger later — same measurement pass can cover both.

### phase E — focus mode — SHIPPED (2026-08-29, see CHANGELOG unreleased). Follow-up idea kept: which MD tabs (monitor/tasks/activity) deserve pokéharness equivalents someday.


## smaller known items

- **garden UI crash — RESOLVED (recovery SHIPPED 2026-08-29, see CHANGELOG unreleased)**: failure mode was confirmed from harness.log (`webgl context lost` 10:59:53Z, `not restored after 10s` 11:00:03Z — pixi 8 self-heal disproven). Auto-rebuild now remounts the scene on the 10s alarm (2 attempts / 60s window, then a click-to-rebuild overlay); battlers respawn from a pre-teardown snapshot. Dossier updated: docs/triage/2026-08-29-garden-ui-crash.md. Residual watch: whether real-world context losses recover cleanly (look for `gpu` area "rebuilding renderer" rows).
- **ResizeObserver error storm**: harness.log has 852 `ResizeObserver loop completed with undelivered notifications` error rows in two bursts (2026-08-28T21:56:30Z, 2026-08-29T11:03:49Z — the second at ~125 rows/sec right after the webgl loss). Indicates a layout feedback loop (suspect: roster strip content-height jitter and/or terminal fit loop) AND spams the log the diagnostics export tails. Fix in two parts: (1) the strip fixed-height fix may kill the loop source; (2) rate-limit/dedupe this specific browser message in the renderer error logger so a storm can't flood the 2MB export tail.
- **battle fx null crash — CLOSED (already fixed pre-session)**: the 2026-08-28T23:37:20Z `tickBattleFx` null-y throw was from a build predating commit a440076, which already added the `entry.owner.destroyed` cull + purge-on-destroy. Verified during the webgl-recovery dispatch; no change needed. If the row ever reappears on a build ≥ a440076, reopen.

- **arceus in the agent bar — SHIPPED (2026-08-29, see CHANGELOG unreleased)**: gold-framed always-present card in the roster strip. Scope note: deliberately NOT added to FocusSidebar/SessionsOverview (bottom bar only, as asked) — extend later if wanted.

- **themed map borders — SHIPPED for default garden (2026-08-29, see CHANGELOG unreleased)**: bush/hedge ring outside the fence, per-map border config (`DEFAULT_GARDEN_BORDER` in mapBorder.ts) ready for future maps. Needs user visual QA: fit zoom now a few % further out; ring crosses the south gate opening (no cut — acceptable?). Future backdrop maps must each ship their own border theme (requirement carried into the backdrops design item).

- **usage section update — SHIPPED (2026-08-29, see CHANGELOG unreleased)**: 5h/session dedupe fixed by root cause (kind-based restatement matching), credit rows added for claude (`extra_usage` gauge) and codex (`credits.balance`, plain text). Follow-up note: codex credit balance UNIT is unverified from a primary source (rendered as a bare number, no `$`) — confirm against a real codex account response someday.

- **AGENT_ID env var may collide with the CLI's own — SHIPPED (2026-08-29, see CHANGELOG unreleased)**: renamed to `POKEHARNESS_AGENT_ID` end-to-end (set in pty.ts, read by the generated hook shim in hookBridge.ts). Only one set site and one read site exist for the bare name (confirmed by repo-wide grep); no dual-name compat needed since the shim script is unconditionally rewritten by `ensureFiles()` at the top of `app.whenReady`, before any pty can spawn, so setter and reader can never disagree even across an app upgrade. Watch item found while tracing this (not fixed, out of scope): hookRouter.ts's `SubagentStop` and BattleManager.ts's header both document that when a subagent-scoped hook's `harness_agent_id` collided with the CLI's own tagging, it landed on an IPC channel nobody listens on and was silently dropped — and hookBridge.ts's own HOOK_SHIM comment says the CLI tags a subagent's own PreToolUse/PostToolUse (not just SubagentStop) with its internal `agent_id`/`agent_type`, so the same collision plausibly hit those too. If so, this rename means a subagent's own tool-call hooks will now resolve `harness_agent_id` to the PARENT's real id instead of colliding — and hookRouter.ts's `PreToolUse`/`PostToolUse` cases have no subagent-awareness (unlike `SubagentStop`, which was deliberately routed around this), so they'd start updating the parent's tool/station display and firing battle 'attack' signals off the subagent's own tool calls, not just the parent's. Unverified either way — no live capture has isolated a subagent-scoped PreToolUse/PostToolUse's `harness_agent_id` value. Watch the next real fan-out's counters/battle log for a parent session's tool indicator flickering with a subagent's tool names, or extra 'attack' beats during a battle that shouldn't be running.
- **garden camera is locked to the selected pokemon — SHIPPED (2026-08-29, see CHANGELOG unreleased)**: free-look landed (empty-click/Esc deselect, wheel zoom, drag pan, re-follow on select). Original finding: `select(null)` is never called anywhere — no empty-ground click, no Esc, no wheel zoom, no drag pan; once a session is selected the camera hard-follows at 2.4x forever and the user cannot see the rest of the map. Fix: free-look controls (deselect via empty-ground click + Esc, wheel zoom, drag pan) with follow resuming on next selection.

- **subagent battlers get no roster presence — SHIPPED (2026-08-29, see CHANGELOG unreleased)**: full-size cards after the parent's card, click navigates to parent terminal. Scope note: RosterStrip shipped first; FocusSidebar + SessionsOverview extension requested 2026-08-29 and dispatched. Original ask: the bottom agent bar only shows real CLI sessions; running subagent pokemon (charmander/larvitar/squirtle etc.) don't appear there, so the user can't tell from the bar what's fighting/working. Add compact subagent chips (sprite + working state) grouped under/after their parent session's card, appearing on materialize and leaving on cleanup.

- **session status: model + context window UI (requested 2026-08-29, design mockups dispatched — HARD GATE: user picks a design from the artifact before ANY implementation)**: since the statusline is disabled in pokeharness terminals, the app needs its own way to show (a) which MODEL a session is using — and model can change mid-session, so it must live-update; (b) a context-window progress bar (like the user's own statusline: `Context: [▓▓ ] 8% 79K/1.0M`); (c) the multitask ⇶ indicator from the old statusline; (d) for claude sessions, fable-scoped limits shown too if applicable (usage popover already has model-scoped 7d fable rows). Open design question the mockups must answer: custom pokeharness statusline strip vs somewhere in the app chrome (topbar/roster/focus header) — and the user wants better UI than the current plain "7d 72%" chip: pixel-art HP-bar-style progress bars in the app's design language. Artifact: session-status-designs — https://claude.ai/code/artifact/2f78f447-1688-44c2-8741-508b633d2032. CONVERGING (2026-08-29 user feedback): composite direction — roster cards get context HP-bar + model badge; terminal statusline strip carries ONLY model + context + multitask (no limit windows); 5h/7d/7d-fable/credits move to a per-session trainer-card POPOVER (anchored to a trigger badge on the roster card, usage-chip-popover pattern); topbar chip upgraded to HP-bar mini-gauge. DESIGN LOCKED (2026-08-29, user): direction 5 composite with three amendments — (1) the statusline strip appears on BOTH the focus-view terminal AND the garden/terminal-split drawer terminal; (2) the context label reads "context", not "ctx"; (3) the topbar replaces the current single text chip with THREE mini HP-bar gauges in the mockup's exact form ([bar] label N%): 5h, 7d, and 7d fable (fable gauge only when the account exposes that window). Trainer-card popover stays as designed (per-session trigger badge on roster cards; houses portrait/model/context/5h/7d/fable/credits/multitask). SHIPPED (2026-08-29, see CHANGELOG unreleased). Deliberate deviations from mockup, all reasonable: trigger badge bottom-left (top-right collides with status pill), reset phrasing reuses "resets in Xh Ym", pixel SVG icon instead of 🎴. Needs user visual QA (hp-bar sizing, badge placement, popover anchoring — math-verified only).

- **friend-testing readiness — SHIPPED (2026-08-29, see CHANGELOG unreleased)**: 8 capture gaps closed, toggle (default on, errors + warns always logged), zip export with ~-redaction, rotation 20MB. Both follow-ups now closed (2026-08-29, see CHANGELOG unreleased): (1) toggle-off no longer suppresses WARN rows (pty nonzero exit, hookBridge, usageService, counters divergence) — warn now logs unconditionally alongside error in diagnostics.ts; (2) hookBridge.ts's malformed-payload log now caps at a 120-char prefix plus parse error + payload length, down from up to 500 raw chars. Original ask: user plans to have friends beta-test the app and wants (a) an audit that logging is detailed and catches ALL error types (main + renderer + promise rejections + pty + gpu — some added only today), filling any gaps; (b) a diagnostics-logging opt-in toggle on the settings diagnostics page, ENABLED by default for now (friends cohort); (c) a dead-simple share path: an "export diagnostics bundle" action that packages recent logs + app/OS version info into one file the tester can send to the user, with obvious-secret redaction considered. Also check log rotation/size so week-long testing doesn't grow unbounded.

- **arceus persona re-sent to an existing chat — SHIPPED (2026-08-29, see CHANGELOG unreleased)**: root causes = missing mid-run resume path + unserialized overlapping summons + stale first-prompt listener; all fixed in arceus.ts only. Original report: user requirement — the persona/system prompt is already in arceus's chat history; as long as arceus isn't being INITIALIZED fresh, it must not be sent again. Code intent already matches (summonArceus = fresh only, resume paths skip persona per arceus.ts comments), so some path is fresh-summoning when it should reuse: suspects = the topbar summon chip on an already-live/persisted arceus, autoSummonArceus fallback, and the new ArceusRosterCard click (merged today). Agent to trace every path that can type the persona and guarantee send-exactly-once per fresh conversation.

- **resumed agents are invisible — SHIPPED (2026-08-29, see CHANGELOG unreleased)**: fixed as part of the battler ↔ task-id correlation work below (same identity plumbing unlocked both). A resumed task-id (dispatched async again after its first battler already fought and faded) now re-materializes a battler with the same species/label and fights a real completion battle again when the resumed run finishes; taskNotificationWatcher.ts's completion dedupe now clears a task-id from its seen-set the moment it sees that exact relaunch, so the resumed run's own completion notification isn't silently swallowed. Guarded against double-spawn if a resume arrives while the original battler is still alive (or still mid-poof). Caveats: (1) the resume-respawn memory (species/label per task-id) is BattleManager's own in-memory map — a resume that happens after an app restart won't have it and stays invisible, same as any other purely in-memory battle state; (2) UNVERIFIED (no live capture of a real resume/SendMessage relaunch) — the whole respawn path assumes a resume writes a FRESH `toolUseResult.isAsync` transcript line carrying the SAME agentId, the way an independent dispatch would. The CLI's own "same task-id may notify more than once" note only documents the notification side re-firing, not that the launch side does too. If that assumption is wrong, the resumed run's completion still isn't swallowed (the dedupe-clear is independent of this), it just falls through to the oldest-roaming fallback instead of a proper respawn — a real but narrower version of this bug.
- **battler ↔ task-id correlation — SHIPPED (2026-08-29, see CHANGELOG unreleased)**: battlers are created at Task PreToolUse where no agentId exists yet; agentId only appears once the parent's transcript shows the matching async-launch line. Fixed via Claude Code's documented `tool_use_id` hook field (present at PreToolUse, before any CLI-internal task-id exists) — threaded onto the battler it spawns, then linked to the real task-id via that same async-launch transcript line (which already carries `tool_use_id` as a sibling of `toolUseResult.agentId`). Completions now retire the exact battler stamped with that task-id (`BattleManager.handleEnd`); the old "oldest roaming" heuristic remains only as a fallback for a battler whose correlation never landed (raced ahead, or predates this fix), and the 30-min age-out backstop (v1.6.0) is unchanged. One link in the chain is unverified against a live transcript capture (this app can't spawn a real interactive `claude` session): that the `tool_result` block's `tool_use_id` really sits alongside `toolUseResult` on the same transcript entry, as the standard Anthropic tool_use/tool_result shape implies — if that assumption is ever wrong for some transcript variant, correlation just never lands for that one dispatch (no crash, oldest-roaming fallback kicks in).

- **battle fix live verification — CONFIRMED IN THE WILD (2026-08-29 ~12:10-12:28 GST, v1.4.0)**: 5-agent fan-out: 5 spawned/materialized/poofed-in, Stop correctly skipped while async agents outstanding (pendingAsyncLaunches 5→1), 4 completion battles started/resolved exactly as each agent finished (durations 9-16 min, so the old 8-min safety timer is provably no longer the trigger), 0 battleSignalErrors. Previous watch item (2026-08-29, headless-only verification): the async-completion watcher was verified against headless `claude -p` captures; still unproven on a live interactive session — whether an idling session's transcript receives a completed async task's notification promptly (vs only on next user prompt). Watch the next real fan-out; log counters (battlesStarted vs notifications) will tell.


- **per-garden backdrops (requested 2026-08-29, designs in SECOND revision)**: v2 verdict (2026-08-29): archetype themes are right, but "the maps look really bland and generic — needs heavy UI work". v3 dispatched: keep the location archetypes, dramatically raise visual richness/detail density (landmarks, decoration, texture, composition — maps that look like real hand-crafted pokemon areas). Include a themed map border per archetype (see themed-map-borders item). Same artifact URL updates in place. Then user picks 5+, then build. v3 VERDICT (2026-08-29): "much better but still quite a bit worse than the default map in terms of quality" — OPEN QUESTION for the user before any v4: the default map is REAL tile art (kenney/grasswater/oga sheets) while the mockups are procedural canvas approximations, so some quality gap is inherent to the medium; options are (a) treat v3 as layout/theme approval only and enforce default-map quality at BUILD time with real/new tile art per archetype, or (b) another mockup polish round first. Await user's call + picks. NOTE: user said "each new session" — assumed each new GARDEN (sessions share one garden view); still unconfirmed.
- **garden day/night cycle (2026-08-29, preview v3 well received — ANIMATION pass dispatched: warm lights get flicker/breathe + subtle sway, moon pool at most a faint breathe, reduced-motion respected; build-note to spec the Pixi per-frame wobble)**: v1 verdict (2026-08-29): "evening tint looks really plain and moonlit night doesnt look good at all — needs heavy UI work". v2 dispatched: rework the sunset and night grades entirely (richer sunset — warm/long-shadow atmosphere, not a flat tint; night = genuinely pretty full-moon look per the original brief: not very dark, moonlight pooling on the ground). HARD GATE unchanged: no garden code until the user approves the preview. Likely implementation once approved: a Pixi color-grade overlay layer interpolating by local time (cheap, phase-F-friendly), not new tile art.


- [x] dock icon white ring on macOS Tahoe — CLOSED (2026-08-29, user eyeballed the real Dock and confirmed fixed). Implementation record: root cause was Tahoe plating any app that ships only a legacy `.icns` onto its system squircle, regardless of bleed. Fix: `build/icon/gen-tahoe-icon.mjs` builds a single-layer Icon Composer (`.icon`) document from the existing full-bleed 1024px art (persisted by `gen-icon.mjs` as `build/icon/icon-1024.png`) and compiles it with `actool` (Xcode 26.6) into a checked-in `build/Assets.car`; package.json's `mac.extraResources` copies it to `Contents/Resources/Assets.car` and `mac.extendInfo.CFBundleIconName` is set to `"PokeharnessIcon"` (matching the name compiled into the asset). `.icns` stays wired as the pre-Tahoe fallback (`CFBundleIconFile`), unchanged. Verified via `electron-builder --dir`: `Assets.car` present in `Contents/Resources` (byte-identical to the checked-in copy), `CFBundleIconName`/`CFBundleIconFile` both correct in the packaged `Info.plist`, `codesign --verify --strict --deep` clean, and `assetutil --info` shows a valid multi-scale "Icon Image" set. Went further than wiring checks: actool's own companion `.icns` byproduct (from `--include-all-app-icons`) turned out to be a red herring — it always renders our layer inset with a visible plate, regardless of the source `.icon` document, because that legacy export path bakes in its own pre-Big-Sur safe-area convention. The signal that actually matters — how Tahoe itself resolves `CFBundleIconName` — was reproduced without launching the app by pointing `Bundle(path:)` + `NSImage`/`bundle.image(forResource:)` (AppKit's own resource lookup, no process launch) at a throwaway bundle wrapping the compiled `Assets.car`: our shipped icon.json (single layer, default positioning) renders full-bleed with no plate, and an artificially scaled-down layer (control test) reproduced the exact white-plate bug through the same pipeline — confirming the test discriminates real plating from a false alarm. Still open only because no one has looked at the actual Dock on this machine yet.

## bigger later

- **windows build (logged 2026-08-29; sequence AFTER the diagnostics-export work merges, so windows testers ship logs from day one)**: today's packaging is arm64 macOS only (zip+dmg, unsigned). Assessment (2026-08-29, code-verified): hookBridge already has win32 branches (`poke-node.cmd`); node-pty/xterm/pixi are windows-fine. Known gaps: (1) shellEnv.ts is darwin/linux-only — windows should inherit process env; (2) hooks.sock is a UDS filesystem path — windows needs the `\\.\pipe\` named-pipe form (small but load-bearing); (3) usage service's keychain read needs its file fallback verified against windows claude code's credential location; (4) Cmd-flavored accelerators need Ctrl equivalents; (5) packaging: nsis target + .ico (icon pipeline is mac-shaped: icns + Assets.car) + release.cjs assumes mac artifacts; (6) unsigned builds hit SmartScreen ("More info → Run anyway" — brief testers). Build via GitHub Actions windows runner (free); REAL bottleneck is verification — no windows machine in the loop, so a windows-owning friend is the test loop for first boots (pty spawn, named pipe, `claude` on PATH are the likeliest breakage points).

- **agent society phase** (carries backlog item 3)
- **demo mode + auto-run showreel + portfolio landing page** with live web embed of the garden engine. NOTE (2026-08-29): before building, research whether a proper product-style video can be made with an already-available AI tool or directly by claude (script + storyboard + scripted screen-capture of demo mode + ffmpeg assembly is the zero-cost baseline) — hard constraint: **free to use only, zero spend**; phase F (performance/battery) runs before this phase.
- **macOS menu bar item (requested 2026-08-29, later stage)**: a `Tray` extra in the app with a pixel-art icon in our pokemon design language (note: menu bar template images are tiny ~18-22pt and traditionally monochrome/template-style — a recognizable 1-bit pixel pokéball/sprite silhouette is the likely sweet spot; test both template and full-color). WHAT IT DOES is an open design decision — candidates to pick from when this comes up: at-a-glance agent statuses (working/idle/needs-you counts with a click-through menu per session), a mini usage-limits readout (codexbar territory — we already have the data service), quick summon/focus actions, or just show/hide the window. Decide scope before building.
- load-test 15+ concurrent chatty sessions (FPS/CPU/IPC), batch output only if measurements warrant
- decide whether the GitHub repo renames to match the app (redirects make it safe)
