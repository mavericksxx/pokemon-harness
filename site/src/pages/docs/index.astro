---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const allDocs = (await getCollection('docs')).sort((a, b) => a.data.order - b.data.order);
const pad = (n: number): string => String(n).padStart(2, '0');
---

<BaseLayout title="field notes · pokéharness" current="docs">
  <div class="wrap notes">
    <p class="kicker">docs</p>
    <h1 class="serif">field notes</h1>
    <p class="lede">everything the garden does, one note per feature.</p>
    <ol class="notes-list">
      {allDocs.map((doc, i) => (
        <li>
          <a href={`/docs/${doc.id}/`}>
            <span class="notes-num">{pad(i + 1)}</span>
            <span class="notes-title serif">{doc.data.title}</span>
            <span class="notes-more" aria-hidden="true">→</span>
          </a>
        </li>
      ))}
    </ol>
  </div>
</BaseLayout>

<style>
  .notes {
    max-width: 760px;
    padding-top: 48px;
  }
  h1 {
    font-weight: 500;
    font-size: clamp(32px, 4.5vw, 46px);
    line-height: 1.1;
    margin: 0 0 10px;
  }
  .lede {
    color: var(--ink-soft);
    margin: 0 0 32px;
  }
  .notes-list {
    list-style: none;
    margin: 0;
    padding: 0;
    border-top: 1px solid var(--rule);
  }
  .notes-list a {
    display: flex;
    align-items: baseline;
    gap: 16px;
    padding: 14px 6px;
    border-bottom: 1px solid var(--rule);
    text-decoration: none;
    color: var(--ink);
  }
  .notes-list a:hover {
    background: var(--moss-wash);
  }
  .notes-num {
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--gold-ink);
    flex: 0 0 22px;
  }
  .notes-title {
    flex: 1;
    min-width: 0;
    font-size: 18px;
    font-weight: 500;
  }
  .notes-more {
    font-family: var(--font-serif);
    color: var(--rust);
  }
</style>
