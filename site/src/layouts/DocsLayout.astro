---
import { getCollection } from 'astro:content';
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  currentId: string;
}

const { title, currentId } = Astro.props;

const allDocs = (await getCollection('docs')).sort((a, b) => a.data.order - b.data.order);
const index = allDocs.findIndex((d) => d.id === currentId);
const prev = index > 0 ? allDocs[index - 1] : undefined;
const next = index >= 0 && index < allDocs.length - 1 ? allDocs[index + 1] : undefined;
const pad = (n: number): string => String(n).padStart(2, '0');
---

<BaseLayout title={`${title} · pokéharness docs`} current="docs">
  <div class="wrap docs-shell">
    <nav class="docs-index" aria-label="docs">
      <details class="docs-index-details" open>
        <summary class="docs-index-title serif">docs</summary>
        <ol>
          {allDocs.map((doc, i) => (
            <li>
              <a href={`/docs/${doc.id}/`} aria-current={doc.id === currentId ? 'page' : undefined}>
                <span class="docs-index-num">{pad(i + 1)}</span>
                <span>{doc.data.title}</span>
              </a>
            </li>
          ))}
        </ol>
      </details>
    </nav>
    <article class="docs-body">
            <h1 class="serif">{title}</h1>
      <div class="prose">
        <slot />
      </div>
      <nav class="docs-pager" aria-label="more docs">
        {prev ? <a href={`/docs/${prev.id}/`}><span>← previous</span>{prev.data.title}</a> : <span />}
        {next && <a class="docs-pager-next" href={`/docs/${next.id}/`}><span>next →</span>{next.data.title}</a>}
      </nav>
    </article>
  </div>
</BaseLayout>

<style>
  .docs-shell {
    display: flex;
    gap: 48px;
    align-items: flex-start;
    padding-top: 40px;
  }

  .docs-index {
    width: 236px;
    flex-shrink: 0;
    position: sticky;
    top: 24px;
  }
  .docs-index-title {
    font-style: italic;
    font-size: 17px;
    color: var(--ink);
    padding-bottom: 10px;
    border-bottom: 1px solid var(--rule);
    list-style: none;
    cursor: default;
  }
  .docs-index-title::-webkit-details-marker {
    display: none;
  }
  .docs-index ol {
    list-style: none;
    margin: 8px 0 0;
    padding: 0;
  }
  .docs-index a {
    display: flex;
    gap: 10px;
    padding: 6px 8px;
    font-size: 14px;
    line-height: 1.4;
    color: var(--ink-soft);
    text-decoration: none;
    border-left: 2px solid transparent;
  }
  .docs-index a:hover {
    color: var(--ink);
  }
  .docs-index a[aria-current='page'] {
    color: var(--ink);
    border-left-color: var(--rust);
    background: var(--moss-wash);
  }
  .docs-index-num {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--gold-ink);
    padding-top: 2px;
  }

  .docs-body {
    flex: 1;
    min-width: 0;
    max-width: 700px;
  }
  .docs-kicker {
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--gold-ink);
    margin: 0 0 10px;
  }
  h1 {
    font-weight: 500;
    font-size: clamp(30px, 4vw, 42px);
    line-height: 1.12;
    margin: 0 0 28px;
  }

  /* The doc body comes through <slot /> from astro:content and doesn't carry
     this file's scoping attribute, hence :global() under .prose. */
  .prose :global(p),
  .prose :global(li) {
    font-size: 16px;
    line-height: 1.7;
    color: var(--ink-soft);
  }
  .prose :global(p) {
    margin: 0 0 18px;
  }
  .prose :global(h2) {
    font-family: var(--font-serif);
    font-weight: 600;
    font-size: 22px;
    color: var(--ink);
    margin: 40px 0 12px;
  }
  .prose :global(h3) {
    font-family: var(--font-serif);
    font-weight: 600;
    font-size: 18px;
    color: var(--ink);
    margin: 28px 0 10px;
  }
  .prose :global(strong),
  .prose :global(em) {
    color: var(--ink);
  }
  .prose :global(a) {
    color: var(--moss);
    text-underline-offset: 3px;
  }
  .prose :global(ul),
  .prose :global(ol) {
    padding-left: 22px;
    margin: 0 0 18px;
  }
  .prose :global(li) {
    margin: 0 0 6px;
  }
  .prose :global(li::marker) {
    color: var(--gold);
  }
  .prose :global(code) {
    font-family: var(--font-mono);
    font-size: 0.86em;
    color: var(--rust);
    background: var(--paper-deep);
    padding: 1px 5px;
    box-shadow: inset 0 0 0 1px var(--rule);
  }
  .prose :global(p code),
  .prose :global(li code) {
    overflow-wrap: anywhere;
  }
  .prose :global(pre) {
    background: var(--term-bg) !important;
    color: var(--term-ink);
    padding: 16px 18px;
    overflow-x: auto;
    margin: 0 0 22px;
    font-size: 13px;
    line-height: 1.6;
  }
  .prose :global(pre code) {
    background: none;
    color: inherit;
    padding: 0;
    box-shadow: none;
  }
  .prose :global(blockquote) {
    margin: 0 0 18px;
    padding: 4px 0 4px 18px;
    border-left: 2px solid var(--rust);
    font-family: var(--font-serif);
    font-style: italic;
  }
  .prose :global(table) {
    display: block;
    overflow-x: auto;
    border-collapse: collapse;
    margin: 0 0 22px;
    font-size: 14px;
  }
  .prose :global(th),
  .prose :global(td) {
    border-bottom: 1px solid var(--rule);
    padding: 6px 12px 6px 0;
    text-align: left;
    color: var(--ink-soft);
  }
  .prose :global(img) {
    height: auto;
  }

  .docs-pager {
    display: flex;
    justify-content: space-between;
    gap: 16px;
    margin-top: 56px;
    padding-top: 20px;
    border-top: 1px solid var(--rule);
  }
  .docs-pager a {
    display: flex;
    flex-direction: column;
    text-decoration: none;
    font-family: var(--font-serif);
    font-size: 17px;
    color: var(--ink);
  }
  .docs-pager a span {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--gold-ink);
    letter-spacing: 0.04em;
  }
  .docs-pager-next {
    text-align: right;
    margin-left: auto;
  }
  .docs-pager a:hover {
    color: var(--moss);
  }

  @media (max-width: 820px) {
    .docs-shell {
      flex-direction: column;
      gap: 24px;
      padding-top: 24px;
    }
    .docs-index {
      position: static;
      width: 100%;
    }
    .docs-index-title {
      cursor: pointer;
    }
    .docs-index-title::after {
      content: ' ▾';
      font-style: normal;
      font-size: 12px;
      color: var(--ink-faint);
    }
  }
</style>

<script>
  // Collapse the index on narrow screens so the note itself comes first.
  const details = document.querySelector<HTMLDetailsElement>('.docs-index-details');
  if (details && window.matchMedia('(max-width: 820px)').matches) details.open = false;
</script>
